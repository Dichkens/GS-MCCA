# 傅里叶图神经网络优化

## 1. 优化目标

本次优化聚焦于项目中 `FourierGNNmodel.py` 的傅里叶图神经网络部分，主要目标是：
- 规范傅里叶域变换结构，减少重复代码并增强可扩展性
- 提高频谱特征变换的可读性和模块化
- 让图网络实际使用边注意力权重 `edge_norm`
- 保留原始谱域结构风格，同时增强残差与稀疏性处理

## 2. 主要改动内容

### 2.1 引入复数线性变换模块

新增 `ComplexLinear`，用于在傅里叶频域上执行复数线性映射。
该模块将实部和虚部分别处理，并统一为一个可学习的复数变换。

### 2.2 引入谱域残差块

新增 `SpectralBlock`，包含：
- 复数线性变换
- ReLU 激活（分别作用于实部与虚部）
- 可选 dropout
- `softshrink` 稀疏化
- 残差连接

这个模块让 `FGN` 的谱域变换更松耦合、可堆叠，也更易于后续扩展。

### 2.3 重构 `FGN` 模块

将原来的三层 `einsum` 计算替换为：
- 统一的 `ComplexLinear` + `SpectralBlock` 堆叠
- 更清晰的频域前馈流程
- 保留原有 `FFT` / `iFFT` 以及投影、分类前处理

优化后 `FGN` 的前向流程为：
1. 词向量扩展到复数嵌入
2. `rfft` 到频域
3. 多个谱域残差块变换
4. 加回频域残差 `bias`
5. `irfft` 返回时域
6. 线性映射到低维并分类

## 3. 图神经网络层面的优化

### 3.1 边注意力权重实际参与节点更新

原始 `GraphNetwork` 只使用了 `RGCNConv`，但 `edge_norm` 计算出的边权重没有实际参与消息传递。

优化后，在 `conv1` 输出后增加了一个 `edge_norm` 加权步骤：
- 根据目标节点 `dst` 聚合边权重
- 计算每个节点的平均边权重
- 使用 `1 + node_scaling` 对卷积输出进行缩放

这样，边注意力就能影响节点特征更新，而不是仅在构造阶段被丢弃。

## 4. 优化效果

- 更清晰的傅里叶谱域网络结构，更符合频域模块化设计
- 更容易扩展层数、调整稀疏性和引入复杂谱变换
- 边注意力 `edge_norm` 不再是冗余输出，而是融入节点特征更新
- `FourierGNNmodel.py` 通过了语法验证，说明改动结构稳定

## 5. 后续建议

1. 继续将 `MaskedEdgeAttention` 的计算优化为更少的 Python 循环，进一步提升构图效率。
2. 如果需要更强的边权建模，可改为自定义 `MessagePassing` 模块，将 `edge_norm` 直接作用于消息函数。
3. 若要进一步增强谱域效果，可在 `SpectralBlock` 中引入多头频谱注意力或可学习阈值。
